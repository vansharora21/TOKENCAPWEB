[
  {
    "command": "tokencap mcp",
    "description": "Start the local Model Context Protocol (MCP) server over stdio or automatically wire host IDE configurations (--init). Keeps a live repository watcher active for background intelligence updates.",
    "example": "tokencap mcp --init",
    "options": [
      { "flag": "--init", "description": "Write workspace configuration file for host IDE automatically." },
      { "flag": "--client <host>", "description": "Specify target host explicitly (vscode, antigravity, codex, cursor, claude, windsurf, cline)." },
      { "flag": "--tools", "description": "List all 11 MCP endpoints and descriptions." },
      { "flag": "--test", "description": "Run self-test: call every endpoint and verify JSON RPC output." },
      { "flag": "--health", "description": "Run one-shot health check without launching the persistent server." }
    ]
  },
  {
    "command": "tokencap brain",
    "description": "Project Brain — unified intelligence query about any part of your codebase (files, risk, architecture, dependencies, review groups, recent changes, and git timeline).",
    "example": "tokencap brain auth",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--impact", "description": "Show affected file/feature count, critical files, and risk level." },
      { "flag": "--architecture", "description": "Render dependency chain ASCII view for the cluster." },
      { "flag": "--review", "description": "Show review group files and reason for grouping." },
      { "flag": "--timeline", "description": "Show git commit history (date + message) for cluster files." },
      { "flag": "--risk", "description": "Show risk areas and affected files." },
      { "flag": "--deps", "description": "Show full depends-on and used-by lists." }
    ]
  },
  {
    "command": "tokencap agent",
    "description": "Analyzes the repository and automatically generates a complete Self-Loading Universal Context Layer (AGENTS.md, START_HERE.md, allowed-context.json, model-instructions.md, context-manifest.json). Use --execution flag to generate the Execution Contract.",
    "example": "tokencap agent --full",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--full", "description": "Run full Universal Context Layer pipeline (default)" },
      { "flag": "--architecture", "description": "Focused generation of tech stack and architecture details" },
      { "flag": "--rules", "description": "Focused generation of project coding rules and conventions" },
      { "flag": "--skills", "description": "Focused generation of agent skill definitions and instructions" },
      { "flag": "--no-agents-file", "description": "Skip generating the root-level AGENTS.md file" },
      { "flag": "--export <target>", "description": "Export rules directly to IDE rulesets (cursor | windsurf | cline | roocode)" },
      { "flag": "--execution", "description": "Also generate the Execution Contract lifecycle suite under .tokencap/agent/execution-contract/" },
      { "flag": "--strict", "description": "Strict mode: enables scope drift confirmation and gating rules (requires --execution)" }
    ]
  },
  {
    "command": "tokencap ask",
    "description": "Smart context retrieval from a natural language question. Auto-detects target mode, retrieves relevant files, traverses the graph (BFS), applies a token budget, and generates a context package and suggested AI prompt.",
    "example": "tokencap ask \"How does authentication work?\"",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--mode <name>", "description": "Override auto-detected mode: review | debug | architecture" },
      { "flag": "--budget <n>", "description": "Custom token budget for the context pack. Default: 12000" },
      { "flag": "--json", "description": "Also export a machine-readable context.json file" },
      { "flag": "--no-prompt", "description": "Skip generating the prompt.md file" }
    ]
  },
  {
    "command": "tokencap context",
    "description": "Manual context generation by topic keyword (e.g. auth, payments, dashboard). Finds files by topic, scores them, performs graph BFS, and generates a context package.",
    "example": "tokencap context auth",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--mode <name>", "description": "Override context mode: review | debug | architecture" },
      { "flag": "--budget <n>", "description": "Custom token budget for the context pack. Default: 8000" },
      { "flag": "--json", "description": "Also export a machine-readable context.json file" },
      { "flag": "--prompt", "description": "Generate a suggested AI prompt.md alongside the context" }
    ]
  },
  {
    "command": "tokencap make",
    "description": "Generate the complete TokenCap snapshot. Incremental by default since v1.3.0. Updates graph, brain, constitution, and agent files by rebuilding only what changed.",
    "example": "tokencap make",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--full", "description": "Force full rebuild (v1.2.0 behavior)" },
      { "flag": "--rebuild-brain", "description": "Rebuild only the Brain knowledge base" },
      { "flag": "--rebuild-constitution", "description": "Rebuild only the Constitution Engine" },
      { "flag": "--rebuild-graph", "description": "Rebuild only the Dependency Graph" },
      { "flag": "--clean-cache", "description": "Clear the incremental cache (next build = full)" },
      { "flag": "--out <path>", "description": "Snapshot output path. Default: TOKENCAP.md" },
      { "flag": "--profile <name>", "description": "Context profile: compact | balanced | deep | gpt-4o | claude-3-5-sonnet | gemini-1.5-flash | gemini-1.5-pro | llama-3-8b" },
      { "flag": "--max-files <n>", "description": "Maximum number of files to include" },
      { "flag": "--max-bytes <n>", "description": "Total source byte budget" },
      { "flag": "--max-file-bytes <n>", "description": "Per-file content byte limit" },
      { "flag": "--max-diff-bytes <n>", "description": "Git diff byte budget" },
      { "flag": "--no-diff", "description": "Skip Git diff snippets" },
      { "flag": "--no-contents", "description": "Skip file contents (structure only)" },
      { "flag": "--no-pointers", "description": "Skip multi-host agent pointer file generation" }
    ]
  },
  {
    "command": "tokencap graph",
    "description": "Generate a code knowledge graph. Maps imports/exports across JS/TS source files and classifies nodes (route, service, component, controller, database, config, etc.) with risk ratings and automatic cluster detection. Note: 'tokencap graph' is deprecated in v1.5. Use: tokencap make --open-graph",
    "example": "tokencap make --open-graph",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--open", "description": "Open Obsidian-style interactive graph viewer in default browser" },
      { "flag": "--full", "description": "Runs full graph intelligence: JSON export + AI summary + graph diff + HTML viewer + opens browser" },
      { "flag": "--minimal", "description": "Generates only graph-data.json, nodes.json, edges.json. Fastest, no HTML or AI summaries" },
      { "flag": "--quiet", "description": "Suppress logs, displaying only a summary of nodes, edges, clusters, and high-impact files" },
      { "flag": "--diff", "description": "Generate graph-diff.md and graph-diff.json comparing current vs previous graph run" },
      { "flag": "--ai", "description": "Generate AI narrative summary only" },
      { "flag": "--json", "description": "Export JSON files only" }
    ]
  },
  {
    "command": "tokencap diff",
    "description": "Perform semantic Change Intelligence analysis on your edits to detect breaking changes, evaluate risk levels (LOW to CRITICAL), map API endpoints, and suggest test cases.",
    "example": "tokencap diff --staged --pr",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--staged", "description": "Analyze only staged changes (prior to a commit)" },
      { "flag": "--last", "description": "Analyze changes in the last commit (HEAD~1..HEAD)" },
      { "flag": "--json", "description": "Export machine-readable analyzed-diff.json" },
      { "flag": "--pr", "description": "Generate PR summary description" },
      { "flag": "--prompt", "description": "Generate optimized AI code review prompt" }
    ]
  },
  {
    "command": "tokencap debug",
    "description": "Manage interactive debug sessions. Captures command failures (stdout/stderr, failed tests, stack traces), tracks changed/related files, and maintains a timestamped session timeline.",
    "example": "tokencap debug --start -- npm test",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--start", "description": "Start a new debug session (append '-- <command>' to auto-capture execution failures)" },
      { "flag": "--end", "description": "End active session, compile the final handoff report, and archive it" },
      { "flag": "--log \"message\"", "description": "Add a timestamped event or discovery note to the active investigation timeline" }
    ]
  },
  {
    "command": "tokencap pack",
    "description": "Compresses the codebase into a token-budgeted, importance-scored context pack for LLMs. If a topic is provided, the pack engine scopes the context to files inside that cluster.",
    "example": "tokencap pack auth --mode review --budget 15000",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "<topic>", "description": "Optional topic or cluster keyword (queries Brain to scope context pack)" },
      { "flag": "--mode <name>", "description": "Select compression mode: review | debug | architecture | minimal" },
      { "flag": "--budget <n>", "description": "Maximum token budget for output pack. Default: 20000" }
    ]
  },
  {
    "command": "tokencap constitution",
    "description": "Manage and inspect your Repository Constitution. Analyzes codebase invariants, schema rules, security boundaries, and API contracts to enforce permanent laws.",
    "example": "tokencap constitution --diff",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--json", "description": "Output raw JSON rule list and graph data" },
      { "flag": "--diff", "description": "Diff current constitution rules against the last run" },
      { "flag": "--history", "description": "Show snapshot history and score tracking over time" },
      { "flag": "--impact <file>", "description": "Query which constitution laws are affected by a specific file" },
      { "flag": "search <query>", "description": "Search constitution rules by keyword, ID, severity, or category" }
    ]
  },
  {
    "command": "tokencap scan",
    "description": "Scans workspace source files for leaked secrets, API keys, private tokens, and high-entropy credentials before snapshot generation.",
    "example": "tokencap scan",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" },
      { "flag": "--quarantine", "description": "Automatically quarantine files containing unredacted secrets" }
    ]
  },
  {
    "command": "tokencap audit",
    "description": "Performs security audit of generated TokenCap intelligence outputs to verify redaction compliance and zero-egress guarantees.",
    "example": "tokencap audit",
    "options": [
      { "flag": "--root <path>", "description": "Workspace root. Default: current directory" }
    ]
  },
  {
    "command": "tokencap upgrade",
    "description": "Checks NPM registry for TokenCap updates and safely upgrades the global package installation.",
    "example": "tokencap upgrade",
    "options": [
      { "flag": "--check", "description": "Check current vs latest version without performing installation" }
    ]
  }
]
